const qualityConfig = [
    {
        rules: {
            complexity: ['error', 10],
            'max-depth': ['error', 2],
            'max-params': ['error', 3],
        },
    },
]

export default qualityConfig